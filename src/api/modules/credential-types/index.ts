/**
 * Ported-frontend adapter.
 *
 * Old's "credential types" are this app's connectors — the catalogue of things
 * you can hold credentials for.
 */
export { useConnectors as useCredentialTypes } from '@/api/modules/connectors';
